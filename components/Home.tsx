'use client';
import { ArrowRight, Sparkles } from 'lucide-react';
import { games } from '@/data/games';
export default function Home({ onSelect }: { onSelect: (id: string) => void }) { return <main className="mx-auto max-w-5xl px-4 pb-16 pt-12 sm:px-6 sm:pt-20"><section className="mb-14 max-w-2xl"><div className="mb-5 inline-flex items-center gap-2 rounded-full bg-[#ffe5d9] px-4 py-2 text-sm font-extrabold text-[#d95448]"><Sparkles size={16}/> 준비 없이, 바로 시작</div><h1 className="text-4xl font-black leading-[1.12] tracking-[-.06em] sm:text-6xl">어색한 분위기를<br/><span className="text-coral">깨는 가장 쉬운 방법</span></h1><p className="mt-5 text-base font-medium leading-relaxed text-[#68727d] sm:text-lg">친구, 동료들과 함께 가볍게 즐기는<br className="sm:hidden"/> 아이스브레이킹 게임</p></section><div className="mb-5 flex items-end justify-between"><h2 className="text-xl font-black">오늘은 뭘 해볼까요?</h2><span className="text-sm font-bold text-[#9a9288]">3가지 게임</span></div><div className="grid gap-4 md:grid-cols-3">{games.map((game, i) => <button key={game.id} onClick={() => onSelect(game.id)} className={`group relative overflow-hidden rounded-[28px] ${game.color} p-6 text-left shadow-soft transition duration-300 hover:-translate-y-1 active:scale-[.98]`}><div className="mb-14 text-7xl transition duration-300 group-hover:scale-110">{game.emoji}</div><div className="relative"><h3 className="text-xl font-black">{game.title}</h3><p className="mt-2 min-h-12 text-sm font-medium leading-relaxed text-[#59616b]">{game.description}</p><div className="mt-5 flex items-center gap-2 text-sm font-black">게임 시작 <ArrowRight size={17} className="transition group-hover:translate-x-1"/></div></div><span className="absolute right-5 top-5 text-xs font-black opacity-40">0{i+1}</span></button>)}</div><p className="mt-10 text-center text-sm font-bold text-[#aaa196]">로그인 없이 · 한 화면으로 · 함께 즐겨요</p></main>; }
