import { ArrowRight, RotateCcw } from 'lucide-react';
import { games } from '@/data/games';
export default function GameIntro({ id, onStart }: { id: string; onStart: () => void }) { const game = games.find(g => g.id === id)!; return <main className="mx-auto flex min-h-[calc(100vh-64px)] max-w-xl items-center justify-center px-4 py-12"><div className={`w-full rounded-[32px] ${game.color} p-8 text-center shadow-soft sm:p-12`}><div className="mb-5 text-8xl">{game.emoji}</div><span className="rounded-full bg-white/70 px-3 py-1 text-xs font-black">GAME {id === 'balance' ? '01' : id === 'crocodile' ? '02' : '03'}</span><h1 className="mt-5 text-3xl font-black">{game.title}</h1><p className="mx-auto mt-4 max-w-sm text-base font-medium leading-relaxed text-[#59616b]">{game.description}</p><button onClick={onStart} className="mt-9 inline-flex min-h-14 items-center gap-3 rounded-2xl bg-ink px-7 text-base font-black text-white shadow-lg transition hover:scale-[1.02] active:scale-95">시작하기 <ArrowRight size={19}/></button><div className="mt-7 flex items-center justify-center gap-2 text-xs font-bold text-[#777066]"><RotateCcw size={14}/> 언제든 다시 시작할 수 있어요</div></div></main>; }
